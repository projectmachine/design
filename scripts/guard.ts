// Lightweight guard kept for the web-only fork. The upstream repository used
// this entry for content, plugin, desktop, and release checks that no longer
// exist here.
console.log('guard: web-only fork checks passed');
